{
  "routes": [
    {
      "method": "GET",
      "path": "/project-todos",
      "handler": "project-todo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-todos/count",
      "handler": "project-todo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-todos/:id",
      "handler": "project-todo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-todos",
      "handler": "project-todo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-todos/:id",
      "handler": "project-todo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-todos/:id",
      "handler": "project-todo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
